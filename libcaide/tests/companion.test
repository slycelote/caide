#!/bin/bash
(sleep 10; echo .) | "$CAIDE" httpServer &
curl -d @request.json -H "Content-Type: application/json" http://localhost:8080
compare_with after-parse GCastleDefense/problem.ini
compare_with after-parse GCastleDefense/case{1,2,3}.{in,out}

